# Comandos Básicos

a = 2 # Variável a

y <- c(3,4,5) # Vetor y

ls() #  Listar variáveis

rm(x) # Remove variável x

rm(list = ls()) # Remove todas as variáveis

z = c(1,2,3,4,5) # Vetor z

sum(z) # Soma dos elementos de z

length(z) # Comprimento de z

sum(z)/length(z) # Média de z

mean(z) # Média de z

var(z) # Variância de z

sd(z) # Desvio padrão de z

median(z) # Mediana de z

quantile(z, 0.5) # Percentil 50 % de z

z[3] #Posição 3 do vetor

z > 3 # Quais elementos de z são maiores que 3
